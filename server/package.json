{
  "name": "@ai-workflow-builder/server",
  "version": "0.1.0",
  "description": "Backend API for AI Workflow Builder — prompt input, grill-me spec refinement, and agent workflow builder.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22.5.0"
  },
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test",
    "test:watch": "node --test --watch",
    "lint": "eslint src test"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "eslint": "^9.9.0"
  }
}
